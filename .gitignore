# Secrets — never commit
.env

# Real (non-redacted) guest data — see CLAUDE.md "Data handling"
emails/
Outcomes.xls
extraction-rules.md
session-*-summary.md

# Personal files that don't belong in the repo
Builder Workbook*.docx

# Python build artifacts
__pycache__/
*.pyc
